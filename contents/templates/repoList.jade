h4= user
each item in items
    div.row 
        div.six.columns
            a(href="#", onClick='repoView("' + item.full_name + '")')
                = item.name
        div.six.columns
            = item.open_issues
            |  issues